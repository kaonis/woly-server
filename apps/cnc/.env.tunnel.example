# WoLy C&C Backend Environment Configuration
# Created for VS Code tunnel development

# Server Configuration
PORT=8080
NODE_ENV=development

# Database Configuration
# DB_TYPE: 'postgrfsfes' or 'sqlite'
DB_TYPE=sqlite

# SQLite: Provide path to database file
DATABASE_URL=./db/woly-cnc.db

# PostgreSQL: Use connection string (when DB_TYPE=postgres)
# DATABASE_URL=postgresql://woly:woly_password@localhost:5432/woly_cnc

# Security - Auth tokens for node agents (comma-separated)
# Add more tokens for additional nodes
NODE_AUTH_TOKENS=dev-token-home,dev-token-office,dev-token-remote

# JWT Secret for API authentication
# IMPORTANT: Change this in production!
JWT_SECRET=dev-secret-key-change-in-production-12345
JWT_ISSUER=woly-cnc
JWT_AUDIENCE=woly-api
WS_REQUIRE_TLS=false
WS_ALLOW_QUERY_TOKEN_AUTH=true

# Node Management
NODE_HEARTBEAT_INTERVAL=30000  # 30 seconds
NODE_TIMEOUT=90000             # 90 seconds

# Logging
LOG_LEVEL=info
